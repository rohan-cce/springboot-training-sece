spring:
  datasource:
    url: jdbc:mysql://localhost:3306/leap_db
    username: root
    password: password # TUTOR NOTE: Have students update this matching their local MySQL Workbench
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # Automatically creates the Database tables dynamically!
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
